apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: order-service-hexagonal-ns
  labels:
    app: kibana
spec:
  type: NodePort
  selector:
    app: kibana
  ports:
    - port: 5601
      targetPort: 5601
      nodePort: 30561
      name: http
